DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
PORT=3000
NODE_ENV=development

# Render / production deploy:
# Build: npm install --include=dev && npm run db:deploy && npm run build
# Start: npm run start:prod
